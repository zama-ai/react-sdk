# 🏗️ Security Architecture Diagrams

> **Visual guides to FHEVM SDK security components**

---

## Table of Contents

- [Key Storage Flow](#key-storage-flow)
- [Threat Model Overview](#threat-model-overview)
- [Storage Security Hierarchy](#storage-security-hierarchy)
- [Phase Implementation Timeline](#phase-implementation-timeline)
- [Encryption Flow (Proposed)](#encryption-flow-proposed)

---

## Key Storage Flow

### Current Architecture (v1.x)

```
┌─────────────────────────────────────────────────────────────────┐
│                         User Action                              │
│                    (Decrypt FHE value)                           │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                    FhevmProvider (React)                         │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Context State:                                          │   │
│  │  - fhevmInstance                                         │   │
│  │  - signature (FhevmDecryptionSignature)                  │   │
│  │  - storage (GenericStringStorage)                        │   │
│  └──────────────────────────────────────────────────────────┘   │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│              FhevmDecryptionSignature Class                      │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Private Fields:                                         │   │
│  │  #privateKey: string    ⚠️ Exposed via public getter     │   │
│  │  #publicKey: string                                      │   │
│  │  #signature: string                                      │   │
│  │  #contractAddresses                                      │   │
│  │  #startTimestamp                                         │   │
│  │  #durationDays (default: 1)                              │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                  │
│  Methods:                                                        │
│  • new() - Generate keypair + sign with wallet                  │
│  • saveToGenericStringStorage() - Serialize to JSON             │
│  • loadFromGenericStringStorage() - Deserialize from JSON       │
│  • isValid() - Check expiration                                 │
│  • toJSON() ⚠️ Includes privateKey in output                    │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│              GenericStringStorage Interface                      │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Methods:                                                │   │
│  │  • getItem(key): string | null                           │   │
│  │  • setItem(key, value)                                   │   │
│  │  • removeItem(key)                                       │   │
│  └──────────────────────────────────────────────────────────┘   │
└───────────────────────────┬─────────────────────────────────────┘
                            │
            ┌───────────────┼───────────────┬──────────────────┐
            │               │               │                  │
            ▼               ▼               ▼                  ▼
    ┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
    │MemoryStorage │ │SessionStorage│ │LocalStorage  │ │ NoOpStorage  │
    │              │ │              │ │              │ │              │
    │ Security:    │ │ Security:    │ │ Security:    │ │ Security:    │
    │ 🟢🟢🟢 HIGH  │ │ 🟢🟢 MEDIUM  │ │ ⚠️ LOW       │ │ 🟢🟢🟢 HIGH  │
    │              │ │              │ │              │ │              │
    │ Cleared on:  │ │ Cleared on:  │ │ Persistent   │ │ No caching   │
    │ Page refresh │ │ Tab close    │ │ (XSS risk)   │ │ Re-sign each │
    └──────────────┘ └──────────────┘ └──────────────┘ └──────────────┘
         ▲                ▲                ▲                  ▲
         │                │                │                  │
         └────── JSON.stringify(signature.toJSON()) ──────────┘
                ⚠️ PLAIN TEXT (no encryption)
```

### Proposed Architecture (v2.0 - After Phase 1-3)

```
┌─────────────────────────────────────────────────────────────────┐
│                    FhevmProvider (React)                         │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  NEW: Security Configuration                             │   │
│  │  - securityLevel: 'strict' | 'production' | 'development'│   │
│  │  - securityLogger: SecurityLogger                        │   │
│  │  - storage: (validated against securityLevel)            │   │
│  └──────────────────────────────────────────────────────────┘   │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│              FhevmDecryptionSignature Class                      │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Private Fields (SEALED):                                │   │
│  │  #privateKey: string    ✅ No public getter              │   │
│  │  #publicKey: string                                      │   │
│  │  #signature: string                                      │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                  │
│  Methods:                                                        │
│  • decrypt(handle, contract) ✅ Uses key internally             │
│  • toJSON() ✅ No privateKey in output                          │
│  • toStorageJSON() 🔒 Private, for EncryptedStorage only        │
│  • rotate() 🆕 Key rotation support                             │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│              GenericStringStorage Interface                      │
│              (+ EncryptedStorageAdapter)                         │
└───────────────────────────┬─────────────────────────────────────┘
                            │
            ┌───────────────┼───────────────┬──────────────────┐
            │               │               │                  │
            ▼               ▼               ▼                  ▼
    ┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
    │MemoryStorage │ │SessionStorage│ │🆕 Encrypted  │ │ NoOpStorage  │
    │              │ │              │ │   Storage    │ │              │
    │ ALLOWED in:  │ │ ALLOWED in:  │ │              │ │ ALLOWED in:  │
    │ All modes    │ │ All modes    │ │ Security:    │ │ All modes    │
    │              │ │              │ │ 🟢🟢🟢 HIGH  │ │              │
    └──────────────┘ └──────────────┘ │              │ └──────────────┘
                                      │ REQUIRED in: │
                                      │ production   │
                                      │ (or session) │
                                      └──────┬───────┘
                                             │
                                             ▼
                                    ┌────────────────┐
                                    │  Web Crypto    │
                                    │  AES-GCM       │
                                    │  Encryption    │
                                    └────────────────┘
```

---

## Threat Model Overview

### Attack Vectors and Mitigations

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           THREAT ACTORS                                  │
└─────────────────────────────────────────────────────────────────────────┘

    1️⃣ XSS Attacker              2️⃣ Malicious Extension      3️⃣ Physical Access
    ┌──────────────┐              ┌──────────────┐            ┌──────────────┐
    │ Injected     │              │ Browser      │            │ Direct       │
    │ JavaScript   │              │ Extension    │            │ Device       │
    └──────┬───────┘              └──────┬───────┘            │ Access       │
           │                             │                    └──────┬───────┘
           │                             │                           │
           ▼                             ▼                           ▼
    ┌──────────────┐              ┌──────────────┐            ┌──────────────┐
    │ Reads        │              │ Reads        │            │ Extracts     │
    │ localStorage │              │ localStorage │            │ Browser      │
    │              │              │ sessionStorage│            │ Profile      │
    │ Exfiltrates  │              │              │            │              │
    │ to attacker  │              │ Logs keys    │            │ Reads disk   │
    └──────┬───────┘              └──────┬───────┘            └──────┬───────┘
           │                             │                           │
           └─────────────┬───────────────┴───────────────────────────┘
                         │
                         ▼
            ┌────────────────────────────┐
            │   MITIGATIONS (Defense)    │
            └────────────────────────────┘

    ✅ Use memoryStorage           ✅ Use EncryptedStorage      ✅ Short expiration
       (cleared on refresh)           (encrypted at rest)          (≤24 hours)

    ✅ Configure CSP headers       ✅ Use sessionStorage        ✅ Clear on logout
       (prevent XSS)                  (cleared on tab close)

    ✅ Audit dependencies          ✅ Never log privateKey      ✅ Key rotation
       (npm audit)                                                (automatic)

    ⚠️ AVOID localStorage in production (unless encrypted)
```

---

## Storage Security Hierarchy

### Risk vs Usability Trade-off

```
                        SECURITY LEVEL (Higher = More Secure)
                                      ▲
                                      │
                        🟢🟢🟢 HIGHEST │
                        ┌─────────────┴──────────────┐
                        │                            │
                        │   memoryStorage            │  ← Best for security
                        │   (re-sign every refresh)  │     Worst for UX
                        │                            │
                        │   Security: ★★★★★          │
                        │   UX:       ★☆☆☆☆          │
                        └────────────────────────────┘
                                      │
                        🟢🟢🟢 HIGHEST │
                        ┌─────────────┴──────────────┐
                        │                            │
                        │   EncryptedStorage         │  ← Best balance
                        │   (AES-GCM encrypted)      │     (Phase 1)
                        │                            │
                        │   Security: ★★★★★          │
                        │   UX:       ★★★★☆          │
                        └────────────────────────────┘
                                      │
                        🟢🟢 MEDIUM   │
                        ┌─────────────┴──────────────┐
                        │                            │
                        │   sessionStorage           │  ← Acceptable
                        │   (cleared on tab close)   │     for production
                        │                            │
                        │   Security: ★★★☆☆          │
                        │   UX:       ★★★☆☆          │
                        └────────────────────────────┘
                                      │
                        ⚠️ LOW        │
                        ┌─────────────┴──────────────┐
                        │                            │
                        │   localStorage             │  ← Development only
                        │   (plain text, persistent) │     XSS vulnerable
                        │                            │
                        │   Security: ★☆☆☆☆          │
                        │   UX:       ★★★★★          │
                        └────────────────────────────┘
                                      │
                                      ▼
                        USABILITY (Higher = Better UX)
```

### Recommended by Environment

```
┌─────────────────┬──────────────────┬────────────────────────────────┐
│  Environment    │  Recommended     │  Rationale                     │
├─────────────────┼──────────────────┼────────────────────────────────┤
│  Development    │  memoryStorage   │  Simple, secure, no state      │
│  (localhost)    │  OR localStorage │  (localStorage for convenience)│
├─────────────────┼──────────────────┼────────────────────────────────┤
│  Staging        │  sessionStorage  │  Cleared on tab close          │
│                 │                  │  (mimics production)           │
├─────────────────┼──────────────────┼────────────────────────────────┤
│  Production     │  Encrypted       │  Secure + persistent           │
│  (v2.0+)        │  Storage         │  (best user experience)        │
├─────────────────┼──────────────────┼────────────────────────────────┤
│  Production     │  sessionStorage  │  Acceptable until encrypted    │
│  (v1.x current) │                  │  storage available             │
├─────────────────┼──────────────────┼────────────────────────────────┤
│  High Security  │  memoryStorage   │  Re-sign every page load       │
│  Apps           │  OR noOpStorage  │  (government, finance)         │
└─────────────────┴──────────────────┴────────────────────────────────┘
```

---

## Phase Implementation Timeline

### Gantt-Style Timeline

```
Months:     1        2        3        4        5        6
            │        │        │        │        │        │
Phase 1:    ████████                                      Non-breaking
(Weeks 1-2) │      │                                      security
            │      └─ Encrypted Storage ✅                improvements
            │         Security Logging ✅
            │         Documentation ✅
            │
Phase 2:             ████████                             Soft-breaking
(Weeks 3-4)          │      │                             API changes
                     │      └─ Deprecate privateKey ⚠️
                     │         Security levels ✅
                     │         Key rotation helpers ✅
                     │
Phase 3:                      ██████████████████████      Breaking v2.0
(Months 3-6)                  │                    │      release
                              │                    │
                    Community testing         Beta │
                    Migration guide            │   └─ v2.0 Release ❌
                    Deprecation warnings ⚠️     │      Remove privateKey
                                               │      Require encryption
                                               └─ RC1

Timeline Summary:
├─ Week 0:  Analysis complete ✅ (2026-02-03)
├─ Week 1-2: Phase 1 implementation
├─ Week 3-4: Phase 2 implementation
├─ Month 2:  Beta testing, gather feedback
├─ Month 3:  Finalize v2.0 plan
├─ Month 4-5: Community migration period
└─ Month 6:  v2.0 release
```

---

## Encryption Flow (Proposed)

### Phase 1: EncryptedStorageAdapter

```
┌─────────────────────────────────────────────────────────────────┐
│                    User Generates Key                            │
└───────────────────────────┬─────────────────────────────────────┘
                            │
              ┌─────────────┴────────────┐
              │                          │
              ▼                          ▼
    ┌─────────────────┐        ┌─────────────────┐
    │ Password-Based  │        │ Device-Bound    │
    │                 │        │                 │
    │ PBKDF2          │        │ generateKey()   │
    │ 100k iterations │        │ Web Crypto API  │
    └────────┬────────┘        └────────┬────────┘
             │                          │
             └──────────┬───────────────┘
                        │
                        ▼
            ┌───────────────────────┐
            │   AES-GCM 256-bit Key │
            │   (CryptoKey object)  │
            └───────────┬───────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────────┐
│               FhevmDecryptionSignature.save()                    │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  1. Serialize to JSON                                    │   │
│  │     { publicKey, privateKey, signature, ... }            │   │
│  │                                                          │   │
│  │  2. Call storage.setItem(key, json)                     │   │
│  └──────────────────────────────────────────────────────────┘   │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│            EncryptedStorageAdapter.setItem()                     │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  1. Generate random IV (12 bytes)                        │   │
│  │     crypto.getRandomValues(new Uint8Array(12))           │   │
│  │                                                          │   │
│  │  2. Encrypt with AES-GCM                                 │   │
│  │     crypto.subtle.encrypt({                              │   │
│  │       name: 'AES-GCM',                                   │   │
│  │       iv: randomIV                                       │   │
│  │     }, key, plaintext)                                   │   │
│  │                                                          │   │
│  │  3. Concatenate: IV + Ciphertext                         │   │
│  │                                                          │   │
│  │  4. Base64 encode                                        │   │
│  │     btoa(iv + ciphertext)                                │   │
│  └──────────────────────────────────────────────────────────┘   │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                  Browser localStorage                            │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Key: "fhevm:enc:sig:0x123..."                           │   │
│  │  Value: "AQIDBAUGBwgJCgsMDQ4P..." (base64)               │   │
│  │                                                          │   │
│  │  🔒 Encrypted - unreadable without key                   │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘

Decryption Flow (Reverse):
1. Read base64 from localStorage
2. Decode from base64
3. Split into IV (first 12 bytes) + ciphertext
4. Decrypt with crypto.subtle.decrypt(AES-GCM, key, ciphertext, iv)
5. Parse JSON
6. Return FhevmDecryptionSignature
```

### Security Properties

```
✅ Authenticated Encryption (AES-GCM)
   └─ Prevents tampering (integrity check)

✅ Unique IV per encryption
   └─ Same plaintext → different ciphertext each time

✅ Key never stored in plaintext
   └─ Either password-derived OR device-bound

✅ Key not extractable (Web Crypto API)
   └─ Can't be exported from browser

⚠️ Still vulnerable if:
   - User device is compromised (keylogger)
   - Weak password used (password-based)
   - User loses device (device-bound key)
```

---

## Data Flow Diagram

### Current (v1.x) - Full Lifecycle

```
┌────────────────────────────────────────────────────────────────────┐
│                         User Opens dApp                             │
└──────────────────────────┬─────────────────────────────────────────┘
                           │
                           ▼
                  ┌────────────────┐
                  │ FhevmProvider  │
                  │ initializes    │
                  └────────┬───────┘
                           │
                           ▼
            ┌──────────────────────────┐
            │ Load signature from      │
            │ storage (if exists)      │
            └──────┬──────────┬────────┘
                   │          │
        ┌──────────┘          └──────────┐
        │ Found                           │ Not found
        ▼                                 ▼
   ┌─────────────┐                  ┌─────────────┐
   │ Deserialize │                  │ User clicks │
   │ from JSON   │                  │ "Decrypt"   │
   └──────┬──────┘                  └──────┬──────┘
          │                                │
          │                                ▼
          │                         ┌──────────────┐
          │                         │ Generate     │
          │                         │ keypair      │
          │                         └──────┬───────┘
          │                                │
          │                                ▼
          │                         ┌──────────────┐
          │                         │ Sign with    │
          │                         │ wallet (EIP) │
          │                         │ -712)        │
          │                         └──────┬───────┘
          │                                │
          │                                ▼
          │                         ┌──────────────┐
          │                         │ Save to      │
          │                         │ storage      │
          │                         └──────┬───────┘
          │                                │
          └────────────┬───────────────────┘
                       │
                       ▼
              ┌────────────────┐
              │ Signature ready│
              │ for decryption │
              └────────┬───────┘
                       │
                       ▼
              ┌────────────────┐
              │ User decrypts  │
              │ FHE value      │
              └────────┬───────┘
                       │
                       ▼
              ┌────────────────┐
              │ Send decrypt   │
              │ request to     │
              │ relayer        │
              └────────┬───────┘
                       │
                       ▼
              ┌────────────────┐
              │ Relayer returns│
              │ plaintext      │
              │ value          │
              └────────────────┘
```

---

## Component Interaction Map

```
┌─────────────────────────────────────────────────────────────────────┐
│                           React Layer                                │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │                      FhevmProvider                             │  │
│  │  • Manages global FHEVM state                                 │  │
│  │  • Provides context to hooks                                  │  │
│  └────────┬──────────────────────────────────────────────────────┘  │
│           │                                                          │
│           ├─► useFhevmContext() ────────────┐                       │
│           ├─► useUserDecrypt() ──────────┐  │                       │
│           ├─► usePublicDecrypt() ─────┐  │  │                       │
│           ├─► useSignature() ──────┐  │  │  │                       │
│           └─► useFhevmInstance() ──│──│──│──│──┐                    │
│                                    │  │  │  │  │                    │
└────────────────────────────────────┼──┼──┼──┼──┼────────────────────┘
                                     │  │  │  │  │
┌────────────────────────────────────┼──┼──┼──┼──┼────────────────────┐
│                          Core Layer │  │  │  │  │                    │
│                                     ▼  ▼  ▼  ▼  ▼                    │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │             FhevmDecryptionSignature                          │  │
│  │  • Holds private key (⚠️ accessible via getter in v1.x)       │  │
│  │  • Manages signature lifecycle                                │  │
│  │  • Serializes to/from storage                                 │  │
│  └───────────┬───────────────────────────────────────────────────┘  │
│              │                                                       │
│              ├─► toJSON() ⚠️ Includes privateKey                    │
│              ├─► isValid() Checks expiration                        │
│              └─► saveToGenericStringStorage()                       │
│                                  │                                   │
└──────────────────────────────────┼───────────────────────────────────┘
                                   │
┌──────────────────────────────────┼───────────────────────────────────┐
│                       Storage Layer                                  │
│                                   ▼                                  │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │             GenericStringStorage                              │  │
│  │  • Interface for key-value storage                            │  │
│  │  • No encryption (v1.x)                                       │  │
│  └───────────┬───────────────────────────────────────────────────┘  │
│              │                                                       │
│      ┌───────┼────────┬──────────┬──────────┐                       │
│      ▼       ▼        ▼          ▼          ▼                       │
│  ┌────┐  ┌────┐   ┌────┐     ┌────┐    ┌────┐                      │
│  │Mem │  │Sess│   │Local│    │NoOp│    │Enc │ (Phase 1)            │
│  │ory │  │ion │   │Stor │    │    │    │rypted│                    │
│  └────┘  └────┘   └────┘     └────┘    └────┘                      │
│   🟢🟢🟢    🟢🟢      ⚠️         🟢🟢🟢      🟢🟢🟢                     │
│                                            │                         │
│                                            ▼                         │
│                                      ┌──────────┐                    │
│                                      │Web Crypto│                    │
│                                      │  AES-GCM │                    │
│                                      └──────────┘                    │
└─────────────────────────────────────────────────────────────────────┘
```

---

## OWASP Compliance Roadmap

### Current vs Target

```
OWASP Guideline                   Current (v1.x)      After Phase 1     After v2.0

Encrypt data at rest              ❌ Plain text       ✅ AES-GCM        ✅ AES-GCM
                                  ════════════════    ════════════════  ════════════════
                                  [░░░░░░░░░░] 0%     [████████░░] 80%  [██████████] 100%

Use authenticated encryption      ❌ No encryption    ✅ AES-GCM        ✅ AES-GCM
                                  ════════════════    ════════════════  ════════════════
                                  [░░░░░░░░░░] 0%     [██████████] 100% [██████████] 100%

Store keys in secure keystore     ❌ Browser storage  ⚠️ Still browser ⚠️ Still browser
                                  ════════════════    ════════════════  ════════════════
                                  [░░░░░░░░░░] 0%     [███░░░░░░░] 30%  [███░░░░░░░] 30%
                                                      (encrypted)       (encrypted)

Implement key rotation            ⚠️ Manual only      ⚠️ Helpers added  ✅ Automated
                                  ════════════════    ════════════════  ════════════════
                                  [███░░░░░░░] 30%    [██████░░░░] 60%  [██████████] 100%

Minimize key exposure time        ✅ 1 day default    ✅ 1 day enforced ✅ Auto-rotate
                                  ════════════════    ════════════════  ════════════════
                                  [████████░░] 80%    [█████████░] 90%  [██████████] 100%

Separate encryption keys          ❌ Stored together  ⚠️ Same storage   ⚠️ Same storage
                                  ════════════════    ════════════════  ════════════════
                                  [░░░░░░░░░░] 0%     [███░░░░░░░] 30%  [████░░░░░░] 40%
                                                      (encrypted)       (separate key)

────────────────────────────────────────────────────────────────────────────────────────
OVERALL COMPLIANCE:               25% (1.5/6)         75% (4.5/6)       85% (5/6)
                                  ════════════════    ════════════════  ════════════════
                                  [██░░░░░░░░] 25%    [███████░░░] 75%  [████████░░] 85%
```

---

## Risk Matrix

### Vulnerability Severity vs Likelihood

```
                    LIKELIHOOD (How likely is this to happen?)
                    ──────────────────────────────────────────►
                    LOW         MEDIUM        HIGH        VERY HIGH

              HIGH  │                        │ 🔴 Plain    │
   SEVERITY        │                        │ localStorage│
      (How         │                        │ (XSS risk)  │
      bad?)   ─────┼────────────────────────┼─────────────┼──────────
                    │ 🔴 Private key        │             │
              MED   │ public getter         │             │
                    │                        │             │
              ─────┼────────────────────────┼─────────────┼──────────
                    │ 🟡 No key rotation    │ 🟡 No       │
              LOW   │                        │ encryption  │
                    │                        │ at rest     │
              ─────┼────────────────────────┼─────────────┼──────────

Legend:
🔴 CRITICAL - Fix immediately (Phase 1)
🟠 HIGH - Fix soon (Phase 2)
🟡 MEDIUM - Plan for fix (Phase 3)
🟢 LOW - Monitor (documentation)

Mitigation Strategy:
└─► Move items LEFT (reduce likelihood) via:
    • User education (docs)
    • Security defaults (no localStorage)
    • CSP headers (prevent XSS)

└─► Move items DOWN (reduce severity) via:
    • Encryption at rest
    • Short expiration times
    • Remove public key access
```

---

<div align="center">

**Security Architecture Documentation**
**Last Updated:** 2026-02-03

[← Back to Security README](./SECURITY_README.md)

</div>
